# 样例数据规则

本目录只能保存合成样例，用来验证接口形状和测试逻辑。

样例数据必须满足：

- ID 使用 `sample`、`demo` 或其他明显占位符。
- 名称不使用真实账户、真实广告组、真实客户、真实员工或真实供应商。
- 金额、数量、日期都是人工构造值。
- 地区、人群、事件名使用通用占位表达。
- 文件中说明它是 synthetic sample data。

不允许放入：

- 平台后台直接导出的 JSON、CSV、Excel。
- 真实截图、抓包、日志和运行数据库。
- 可以反推出真实账户结构或业务规模的样例。
